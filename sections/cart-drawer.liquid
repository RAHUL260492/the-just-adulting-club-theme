
<div class="section-cart-drawer">
  {% if section.settings.heading %}
    <h2>{{ section.settings.heading }}</h2>
  {% endif %}
  <!-- Content for cart-drawer -->
</div>
{% schema %}
{
  "name": "cart drawer",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "cart drawer"
    }
  ],
  "presets": [
    {
      "name": "cart drawer"
    }
  ]
}
{% endschema %}
