
<div class="section-image-banner">
  {% if section.settings.heading %}
    <h2>{{ section.settings.heading }}</h2>
  {% endif %}
  <!-- Content for image-banner -->
</div>
{% schema %}
{
  "name": "image banner",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "image banner"
    }
  ],
  "presets": [
    {
      "name": "image banner"
    }
  ]
}
{% endschema %}
