
<div class="section-main-addresses">
  {% if section.settings.heading %}
    <h2>{{ section.settings.heading }}</h2>
  {% endif %}
  <!-- Content for main-addresses -->
</div>
{% schema %}
{
  "name": "main addresses",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "main addresses"
    }
  ],
  "presets": [
    {
      "name": "main addresses"
    }
  ]
}
{% endschema %}
