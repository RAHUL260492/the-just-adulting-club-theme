
<div class="section-main-order">
  {% if section.settings.heading %}
    <h2>{{ section.settings.heading }}</h2>
  {% endif %}
  <!-- Content for main-order -->
</div>
{% schema %}
{
  "name": "main order",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "main order"
    }
  ],
  "presets": [
    {
      "name": "main order"
    }
  ]
}
{% endschema %}
