
<div class="section-main-password">
  {% if section.settings.heading %}
    <h2>{{ section.settings.heading }}</h2>
  {% endif %}
  <!-- Content for main-password -->
</div>
{% schema %}
{
  "name": "main password",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "main password"
    }
  ],
  "presets": [
    {
      "name": "main password"
    }
  ]
}
{% endschema %}
