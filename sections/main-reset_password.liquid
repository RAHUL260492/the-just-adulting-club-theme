
<div class="section-main-reset_password">
  {% if section.settings.heading %}
    <h2>{{ section.settings.heading }}</h2>
  {% endif %}
  <!-- Content for main-reset_password -->
</div>
{% schema %}
{
  "name": "main reset_password",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "main reset_password"
    }
  ],
  "presets": [
    {
      "name": "main reset_password"
    }
  ]
}
{% endschema %}
