
<div class="section-video">
  {% if section.settings.heading %}
    <h2>{{ section.settings.heading }}</h2>
  {% endif %}
  <!-- Content for video -->
</div>
{% schema %}
{
  "name": "video",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "video"
    }
  ],
  "presets": [
    {
      "name": "video"
    }
  ]
}
{% endschema %}
